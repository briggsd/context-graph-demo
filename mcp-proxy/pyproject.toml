[project]
name = "mcp-proxy"
version = "0.1.0"
description = "Bearer-auth proxy in front of the neo4j-agent-memory MCP SSE server"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.30.0",
]
