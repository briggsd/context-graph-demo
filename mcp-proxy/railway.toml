$schema = "https://railway.com/railway.schema.json"

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-9091}"
restartPolicyType = "ON_FAILURE"
